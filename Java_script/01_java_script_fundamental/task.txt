What is a variable?
a variable is simply the name of storage location. 
  Variable names are case sensitive; “a” & “A” is different
  Only letters, digits, underscore( _ ) and $ is allowed. (not even space)
  only a letter underscore(_) or $ should be 1st character.
  Reserved words cannot be variable.


  let key word:
  syntax of declaring variable...Variable can be re-declared & updated. A global scope variable

  Example if we declared a variable inside braket we cann't use it outer anywhere:.
  {
  let x = 2;
}
  
  var key word:
  variable cannt' be re-declared but can be updated. a block scope variable. 
  we can use it outers anywhere

  const :
  variable cannot be re-declared or updated a bloack scope variable
  means once you declared anything you can't change anything on it.
  we cann't use it outer anywehre 
  once we declared any value on const varialbe we cann't change it to anyother time


  Asignment Operator
  binary Operator
  aga = age +1 or aga += 1 similarly we can write it in negitive case. 

  unary operators 
   age ++
   age --



nul & undefined in js


undefined:
a variable that has not been assigned a value is of tye undefined.

null:
The null value represents the intentional absence of any object value.
